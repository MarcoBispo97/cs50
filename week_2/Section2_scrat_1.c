#include "cs50.h"
#include <stdio.h>
#include <string.h>

int main(void)
{
   int nights[5] = { 7,
                     8,
                     9,
                     6,
                     7,
                     8 }
   // Em C não mudamos o tamanho do array
   // É possível colocar um array dentro do outro
   // Não existe indice negativo em C
}